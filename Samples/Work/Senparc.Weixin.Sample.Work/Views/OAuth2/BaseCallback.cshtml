@model Senparc.Weixin.Work.AdvancedAPIs.MailList.GetMemberResult
@using Senparc.Weixin.Helpers
@using Senparc.CO2NET.Extensions

@*DPBMARK_FILE MP*@
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>OAuth2.0授权测试授权成功</title>
</head>
<body>
    <h2>OAuth2.0授权测试授权成功！</h2>
    @if (ViewData.ContainsKey("ByBase"))
    {
        <p><strong>您看到的这个页面来自于snsapi_base授权，因为您已关注本微信，所以才能查询到详细用户信息，否则只能进行常规的授权。</strong></p>
    }
    else
    {
        <p><strong>您看到的这个页面来自于snsapi_userinfo授权，可以直接获取到用户详细信息。</strong></p>
    }
    <p>下面是通过授权得到的您的部分个人信息：</p>
    <p>userid:@Model.userid</p>
    <p>address:@Model.address</p>
    <p>alias:@Model.alias</p>
    <p>avatar:@Model.avatar</p>
    <p><img src="@Model.avatar" width="128" /></p>
    <p>biz_mail:@Model.biz_mail</p>
    <p>department:@Model.department.ToJson()</p>
    <p>direct_leader:@Model.direct_leader.ToJson()</p>
    <p>email:@Model.email</p>
    <p>enable:@Model.enable</p>
    <p>english_name:@Model.english_name</p>
    <p>extattr:@Model.extattr</p>
    <p>external_position:@Model.external_position</p>
    <p>external_profile:@Model.external_profile</p>
    <p>gender:@Model.gender</p>
    <p>main_department:@Model.main_department</p>
    <p>mobile:@Model.mobile</p>
    <p>name:@Model.name</p>
    <p>open_userid:@Model.open_userid</p>
    <p>order:@Model.order.ToJson()</p>
    <p>position:@Model.position</p>
    <p>qr_code:@Model.qr_code</p>
    <p>status:@Model.status</p>
    <p>telephone:@Model.telephone</p>
    <p>wxplugin_status:@Model.wxplugin_status</p>
</body>
</html>
